// Copy to secrets.h and fill in values for local development / test flashing.
// DO NOT commit secrets.h — it is in .gitignore.
// Production units have these values written to NVS at factory; the firmware
// loads them via identity/provisioning.h at runtime.
#pragma once

#define DEV_MQTT_BROKER   "xxxxxxxx.s1.eu.hivemq.cloud"
#define DEV_MQTT_PORT     8883
#define DEV_MQTT_USER     "dzobibi-dev-device"
#define DEV_MQTT_PASS     "change-me"

#define DEV_GSM_APN       "internet"   // Vodafone UK
#define DEV_GSM_USER      ""
#define DEV_GSM_PASS      ""

// Optional WiFi fallback (leave blank to disable)
#define DEV_WIFI_SSID     ""
#define DEV_WIFI_PASS     ""
